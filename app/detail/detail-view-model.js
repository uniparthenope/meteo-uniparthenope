var observableModule = require("data/observable");

function DetailViewModel() {
    var detailModel = observableModule.fromObject({
    });

    return detailModel;
}

module.exports = DetailViewModel;